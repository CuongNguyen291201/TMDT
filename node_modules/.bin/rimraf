#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<< HEAD
  "$basedir/node"  "$basedir/../@mapbox/node-pre-gyp/node_modules/rimraf/bin.js" "$@"
  ret=$?
else 
  node  "$basedir/../@mapbox/node-pre-gyp/node_modules/rimraf/bin.js" "$@"
=======
  "$basedir/node"  "$basedir/../rimraf/bin.js" "$@"
  ret=$?
else 
  node  "$basedir/../rimraf/bin.js" "$@"
>>>>>>> main
  ret=$?
fi
exit $ret
